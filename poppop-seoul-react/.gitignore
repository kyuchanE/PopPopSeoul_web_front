############################################
# OS / Editor junk
############################################

# macOS Finder metadata
.DS_Store
.AppleDouble
.LSOverride

# macOS thumbnail / indexing files
._*
.Spotlight-V100
.Trashes

# VSCode project settings (개인 개발자 설정은 커밋 안 함)
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/launch.json
!.vscode/tasks.json

# JetBrains / WebStorm 등
.idea/

############################################
# Node / JS / TS common
############################################

# Node_modules (프론트/백엔드 등 전체 하위 디렉토리 포함)
node_modules/
**/node_modules/

# lockfile 백업 등
package-lock.json.*
yarn.lock.*
pnpm-lock.yaml.*

# 빌드 산출물
dist/
build/
.out/
.next/
.next/
.next.cache/
.eslintcache
.cache/
.tmp/
temp/
tmp/

# React / Vite / Next 등에서 생성되는 빌드 결과물
**/dist/
**/build/
**/.next/
**/.cache/
**/coverage/

# TypeScript build output
*.tsbuildinfo

############################################
# Runtime / logs
############################################

# Log files
logs
*.log
**/logs
**/*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
debug.log*

# PID files
pids
*.pid
*.seed
*.pid.lock

############################################
# Env / Secrets
############################################

# 절대 비밀 키, 토큰 등은 커밋하지 않는다
.env
.env.*
*.env
**/.env
**/.env.*

# 로컬만 쓰는 환경 설정 예) .local.json, .local.ts 등
*.local.json
*.local.ts
*.local.js
**/*.local.json
**/*.local.ts
**/*.local.js

############################################
# Testing / coverage / reports
############################################

coverage/
**/coverage/
.nyc_output/
jest-html-reporters-*.html
junit.xml

############################################
# Misc / tooling
############################################

# Docker runtime junk (컨테이너 내부에서 생성되는 경우)
*.tar
*.tmp
docker-data/
**/docker-data/

# PM2 / process manager dumps (백엔드용)
pids
pids/*
*.pid
*.pid.lock
*.dmp

# Local TLS keys / certs for dev
certs/
*.pem
*.key
*.crt

# Generated docs / storybook static builds
storybook-static/
**/storybook-static/

# OS swap / editor swap
*.swp
*.swo
*~